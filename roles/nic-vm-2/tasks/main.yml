---
- name: Creation de la NIC VM-2
  azure_rm_networkinterface:
    name: "{{ nic_name_vm_2 }}"
    resource_group: "{{ rg_name }}"
    virtual_network: "{{ vnet_name }}"
    subnet_name: "{{ subnet_name }}"
    security_group: "{{ nsg_win_name }}"
    ip_configurations:
      - name: ipconfig1
        public_ip_address_name: "{{ public_ip_vm_2 }}"
        primary: True
...