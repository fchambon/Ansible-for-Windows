---
- name: installation de la VM-2
  azure_rm_virtualmachine:
    name: "{{ vm_name_vm_2 }}"
    resource_group: "{{ rg_name }}"
    vm_size: "{{ vm_size_vm_2 }}"
    managed_disk_type: "{{ vm_type_disk_vm_2 }}"
    admin_username: "{{ admin_user_vm_2 }}"
    admin_password: "{{ password_vm_2 }}"
    network_interfaces: "{{ nic_name_vm_2 }}"
    os_type: Windows
    image:
      offer: "{{ offer_vm_2 }}"
      publisher: "{{ publisher_vm_2 }}"
      sku: "{{ sku_vm_2 }}"
      version: "{{ version_vm_2 }}"
...